// const path = require('path');
// path.resolve()
const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-path': resolve(__dirname, 'src', 'models'), // path of models
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), // path of migrations (to database changes)
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),
};
